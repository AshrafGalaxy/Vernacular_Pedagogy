# Local Lightweight Data & Verification Pipeline (CPU-only, no heavy training)
pandas>=2.0.0

# Optional image-generation providers used by scripts/generate_image_assets.py
openai>=1.0.0
google-genai>=1.0.0

# Note: Cloud GPU dependencies (transformers, torch, peft, ctranslate2, piper-tts)
# are configured inside the cloud notebooks (Google Colab / Kaggle).
